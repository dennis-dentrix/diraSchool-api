services:
  mongodb:
    image: mongo:7
    container_name: diraschool-mongo
    ports:
      - '27017:27017'
    volumes:
      - mongo_data:/data/db
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: diraschool-redis
    ports:
      - '6379:6379'
    restart: unless-stopped

  mongo-express:
    image: mongo-express
    container_name: diraschool-mongo-ui
    ports:
      - '8081:8081'
    environment:
      ME_CONFIG_MONGODB_URL: mongodb://mongodb:27017
      ME_CONFIG_BASICAUTH: 'false'
    depends_on:
      - mongodb
    restart: unless-stopped

volumes:
  # Diraschool data volume — persists MongoDB data across container restarts
  mongo_data:
